export function ErrorNote() {
  return (
    <p className="rounded-xl bg-cream-raised px-5 py-4 text-sm text-ink-soft">
      Apple Music is being quiet right now — the catalogue couldn’t load. Try a refresh in a minute.
    </p>
  )
}
